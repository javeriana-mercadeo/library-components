export function DetalleProyecto({ proyecto, onClose }) {
  const project = proyecto || {
    tituloModal: 'Título del Proyecto',
    fecha: 'Fecha del Proyecto',
    responsable: 'Responsable del Proyecto',
    descripcionCompleta: 'Descripción completa del proyecto.'
  }
}
